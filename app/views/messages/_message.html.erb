<div id="message-<%= message.id %>" style="background-color: gray;" class="px-2 py-1 m-2 w-50 rounded-3">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
